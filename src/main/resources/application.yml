server:
  port: '8888'

management:
  security:
    enabled: false
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/leoeberts/taskboards-configuration
          force-pull: true
                  
    
    
#TODO: bus: enabled:true
#TODO: rabbitmq: host: localhost port: 5672  