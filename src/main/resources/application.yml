spring:
  profiles: prod
  cloud:
    config:
      server:
        git:
          uri: https://github.com/leoeberts/taskboards-configuration
          force-pull: true   

name: prod-YAML
environment: production

eureka:
  instance:
    hostname: ${DOMAIN_NAME}
    nonSecurePort: 80
    secure-port-enabled: true
    non-secure-port-enabled: false
    statusPageUrl: https://${DOMAIN_NAME}/info
    healthCheckUrl: https://${DOMAIN_NAME}/health
    homePageUrl: https://${DOMAIN_NAME}/
  password: ${USER_PASSWORD:password}

---
spring:
  profiles: test
  cloud:
    config:
      server:
        git:
          uri: https://github.com/leoeberts/taskboards-configuration
          force-pull: true

name: test-YAML
environment: test
